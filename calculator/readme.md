rules for displaying numbers on screen
- numbers without fractions are displayed without a fractional point
- fractional numbers are displayed with the maximum number of digits available on the display (a number to be decided for display length)
- an algorithm is designed to enable the display of larger numbers with the e^ notation
